@model IEnumerable<Employee>

@{
    ViewBag.Title = "Home Page";
}

@*<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Department</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var emploee in Model)
        {
            <tr>
                <td> @emploee.Id</td>
                <td> @emploee.Name</td>
                <td> @emploee.Emial</td>
                <td> @emploee.Department</td>
            </tr>
        }
    </tbody>
</table>*@


<div class="card-deck">
    @foreach (var employee in Model)
    {


        var photoPath = "~/images/" + (employee.PhotoPath ?? "image_1.png");

    <div class="card m-lg-3" style="min-width:18rem;max-width:30.5%;">
        <div class="card-header">
            <h3>@employee.Name</h3>
        </div>
        <img class="card-img-top" height="150px" src="@photoPath"  asp-append-version="true"/>
        <div class="card-footer text-center">
            <a asp-controller="home" asp-action="details" asp-route-id="@employee.Id" class="btn btn-primary ">View</a>
            <a class="btn btn-primary" asp-action="edit" asp-controller="home" asp-route-id="@employee.Id">Edit</a>
            <a href="#" class="btn btn-danger ">Delete</a>
        </div>
    </div>
    }
</div>
